{
  "name": "dfi",
  "version": "1.0.0",
  "description": "DFI - Detection Fingerprint Identification",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rmdir /q /s dist && parcel build manifest.json --config @parcel/config-webextension"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/config-webextension": "^2.10.3",
    "chrome-types": "^0.1.246",
    "parcel": "^2.10.3",
    "typescript": "^5.3.2"
  }
}
